# Optional dependencies for local development or full feature testing.
# The base package intentionally has no required third-party dependencies.
#
# Install feature groups instead of this file when possible:
#   pip install damei[array]
#   pip install damei[cv]
#   pip install damei[nn]
#   pip install damei[rsa]

numpy
easydict
opencv-python
tqdm
# torch
# torchvision
# pillow
# rsa
